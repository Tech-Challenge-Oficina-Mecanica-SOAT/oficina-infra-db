name: Terraform Apply

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Ambiente para aplicar'
        required: true
        default: 'homolog'
        type: choice
        options:
          - homolog
          - prod

jobs:
  terraform-apply:
    name: Terraform Apply - ${{ inputs.environment || 'homolog' }}
    runs-on: ubuntu-latest
    
    environment:
      name: ${{ inputs.environment || 'homolog' }}
    
    permissions:
      contents: read
      pull-requests: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.0

      # Configuração para AWS Academy (credenciais diretas + session token)
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ secrets.AWS_REGION || 'us-east-1' }}

      - name: Verify AWS identity
        run: |
          echo "🔍 Verificando identidade AWS..."
          aws sts get-caller-identity
          echo "✅ Autenticado com sucesso!"

      - name: Terraform Init
        run: |
          cd envs/${{ inputs.environment || 'homolog' }}
          terraform init

      - name: Terraform Plan
        id: plan
        run: |
          cd envs/${{ inputs.environment || 'homolog' }}
          terraform plan -out=tfplan
        continue-on-error: false

      - name: Terraform Apply
        run: |
          cd envs/${{ inputs.environment || 'homolog' }}
          terraform apply -auto-approve tfplan